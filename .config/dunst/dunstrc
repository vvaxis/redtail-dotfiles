# ============================================================================
# DUNST — redtail notifications v2
# Quente, discreto, Iosevka Nerd Font
# ============================================================================

[global]
    # ── Layout ────────────────────────────────────────────────────────────
    monitor = 0
    follow = keyboard
    width = (200, 380)
    height = 150
    origin = top-right
    offset = 15x15
    notification_limit = 4
    progress_bar = true
    progress_bar_height = 8
    progress_bar_frame_width = 1
    progress_bar_min_width = 150
    progress_bar_max_width = 300

    # ── Aparência ─────────────────────────────────────────────────────────
    transparency = 10
    padding = 12
    horizontal_padding = 14
    text_icon_padding = 12
    frame_width = 2
    frame_color = "#4d3b28"
    gap_size = 4
    separator_color = frame
    corner_radius = 10

    # ── Tipografia ────────────────────────────────────────────────────────
    font = Iosevka Nerd Font 11
    line_height = 2
    markup = full
    format = "<b>%s</b>\n%b"
    alignment = left
    vertical_alignment = center
    word_wrap = yes
    ellipsize = end

    # ── Ícones ────────────────────────────────────────────────────────────
    icon_position = left
    min_icon_size = 24
    max_icon_size = 48
    icon_theme = Papirus-Dark
    enable_recursive_icon_lookup = true

    # ── Comportamento ─────────────────────────────────────────────────────
    sort = yes
    idle_threshold = 120
    show_age_threshold = 30
    stack_duplicates = true
    hide_duplicate_count = false
    show_indicators = yes
    sticky_history = yes
    history_length = 50

    # ── Ações ─────────────────────────────────────────────────────────────
    mouse_left_click = do_action, close_current
    mouse_middle_click = close_all
    mouse_right_click = close_current

    # ── Wayland ───────────────────────────────────────────────────────────
    layer = overlay
    force_xwayland = false


# ── Urgência: baixa (informação, confirmações) ───────────────────────────
[urgency_low]
    background = "#2a1f14"
    foreground = "#b0a48e"
    frame_color = "#4d3b28"
    highlight = "#8aab72"
    timeout = 5

# ── Urgência: normal (notificações padrão) ───────────────────────────────
[urgency_normal]
    background = "#2a1f14"
    foreground = "#e2d5c0"
    frame_color = "#d4a243"
    highlight = "#d4a243"
    timeout = 8

# ── Urgência: crítica (alertas importantes) ──────────────────────────────
[urgency_critical]
    background = "#3b2d1e"
    foreground = "#e2d5c0"
    frame_color = "#c25d5d"
    highlight = "#c25d5d"
    timeout = 0

# ── Regras por app ───────────────────────────────────────────────────────
[spotify]
    appname = Spotify
    frame_color = "#8aab72"
    timeout = 4

[brave]
    appname = Brave
    frame_color = "#c46d4d"
